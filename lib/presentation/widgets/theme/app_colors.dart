import 'package:flutter/material.dart';

class AppColors {
  // Common Colors
  static const primaryColor = Color.fromARGB(255, 206, 0, 0);
  static const accentColor = Color(0xFFF95454);
  // Dark Theme Colors
  static const darkBackground = Color(0xFF222831);
  static const darkPrimaryCard = Color.fromARGB(255, 241, 244, 248);
  static const darkPrimaryText = Color(0xFFEEEDEB);
}
